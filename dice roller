<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dice Roller</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="cont">
    <h1>Dice Roller</h1>
    <label>Number of dice</label>
    <input type="number" id="num" min="1" value="1">
    <button onclick="roller()">Roll Dice</button>
    <div id="res"></div>
    <div id="img"></div>
  </div>

  <script src="work.js"></script>
</body>
</html>

  
 //css
  #cont {
  text-align: center;
  margin-top: 50px;
  font-size: 2rem;
}

h1 {
  color: rgb(84, 29, 136);
}

button {
  font-size: 1em;
  padding: 10px 15px; /* ✅ space, not comma */
  background-color: salmon;
  cursor: pointer;
  border: none;
  border-radius: 8px;
}

button:hover {
  background-color: rgb(255, 160, 122);
}

#num {
  width: 200px;
  font-size: 1em;
  text-align: center;
}

#img img {
  width: 100px;
  margin: 10px;
}


//js
function roller() {
  const numb = document.getElementById("num").value;
  const res = document.getElementById("res");
  const img = document.getElementById("img");

  const val = [];
  const imgva = [];

  for (let i = 0; i < numb; i++) {
    const roll = Math.floor(Math.random() * 6) + 1;
    val.push(roll);
    imgva.push(`<img src="dice_img/${roll}.png" alt="dice${roll}" width="100">`);
  }

  res.textContent = `You rolled: ${val.join(", ")}`;
  img.innerHTML = imgva.join(" ");
}
